{
  "name": "mdv-monorepo",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "bun --cwd packages/cli run mdv",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "coverage": "turbo run coverage",
    "release:version": "bunx changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "c8": "^10.1.3",
    "turbo": "^2.5.6"
  }
}

# Changesets

Use changesets for every release-impacting change.

Add a changeset:
```bash
bunx changeset
```

Version packages:
```bash
bun run release:version
```

Publish packages:
```bash
bun run release:publish
```
